class BinarySearch(int arr[], int 1, int r, int x) {
  
  if(r >= 1) {                    //jiika r lebih dari sama dengan 1 maka jalankan code dibawahnya
    int mid = 1 + (r - 1) /2;     //
    
    if(arr(mid) == x)             //jika arr dari mid sama dengan x maka kembalikan data ke mid
      return mid;
  }
}
